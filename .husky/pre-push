#!/bin/sh

echo "⏳ Running pre-push checks..."

echo "→ TypeScript..."
pnpm typecheck || exit 1

echo "→ Lint..."
pnpm lint || exit 1

echo "→ Tests..."
pnpm test || exit 1

echo "✅ All checks passed"
